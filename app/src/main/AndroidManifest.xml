<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ikbar.locmart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AlfmartActivity"
            android:label="@string/alfamart"></activity>
        <activity
            android:name=".IndomartActivity"
            android:label="@string/indomart"></activity>
        <activity
            android:name=".GiantActivity"
            android:label="@string/giant"></activity>
        <activity
            android:name=".HypermartActivity"
            android:label="@string/hypermart"></activity>
        <activity
            android:name=".LottemartActivity"
            android:label="@string/lottemart"></activity>
        <activity
            android:name=".SuperindoActivity"
            android:label="@string/superindo"></activity>

    </application>

</manifest>